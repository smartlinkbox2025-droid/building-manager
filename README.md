# إدارة العمارة السكنية PWA

تطبيق عربي يعمل دون خادم لإدارة الشقق والسكان والاشتراكات والدفعات والإيرادات والمصروفات والصيانة والتقارير.

## التشغيل محلياً

```bash
npm install
npm run dev
```

## بناء نسخة الإنتاج

```bash
npm run build
```

سيتم إنشاء مجلد `dist` الجاهز للنشر.

## النشر على GitHub Pages

1. أنشئ مستودعاً جديداً في GitHub.
2. ارفع جميع ملفات المشروع إلى الفرع `main`.
3. افتح Settings > Pages.
4. اختر Source: GitHub Actions.
5. سيعمل ملف `.github/workflows/deploy.yml` تلقائياً.
6. سيظهر رابط التطبيق بعد اكتمال النشر.

## التخزين

تُحفظ البيانات داخل IndexedDB في متصفح الجهاز. حذف بيانات المتصفح قد يحذف بيانات التطبيق، لذلك استخدم النسخة الاحتياطية ZIP بانتظام من صفحة الإعدادات.

## تثبيت التطبيق

بعد فتح الرابط في Chrome أو Edge، استخدم خيار "تثبيت التطبيق" أو "إضافة إلى الشاشة الرئيسية".

## ملاحظات

- العملة الافتراضية: الريال السعودي SAR.
- يعمل التطبيق أوفلاين بعد أول تحميل ناجح.
- ملفات PDF تُنشأ من محتوى HTML كصورة لضمان ظهور العربية بصورة سليمة دون الحاجة إلى خطوط خارجية.


## الإصدار الحالي
1.8.0 — الأداء وPWA وGitHub Pages.


## آخر تحديث
1.8.0 — التحميل الكسول، الحماية من الأعطال، العمل دون اتصال، والتجهيز الديناميكي لـGitHub Pages.

لتغيير اسم المستودع لا يلزم تعديل الكود عند النشر عبر GitHub Actions؛ يتم تمرير اسم المستودع تلقائياً. للبناء المحلي لمسار مختلف استخدم `VITE_REPOSITORY_NAME` أو `VITE_BASE_PATH`.

## التحقق قبل النشر

نفّذ الأوامر التالية قبل رفع أي إصدار:

```bash
npm install
npm run validate
npm run check
```

يقوم `npm run validate` بفحص بنية المشروع، إصدار التطبيق، قاعدة البيانات، الجداول، المسارات، إعداد PWA وملفات GitHub Pages قبل بدء بناء الإنتاج.


## الإصدار النهائي

الإصدار الحالي: **2.0.0 Enterprise Final**.

الفحص البنيوي يعمل عبر:

```bash
npm run validate
```

والتحقق الكامل في بيئة متصلة بالإنترنت:

```bash
npm install
npm run check
```

راجع `docs/15_PHASE_10_ENTERPRISE_FINAL_QA.md` لمعرفة ما تم التحقق منه آلياً واختبارات الأجهزة الحقيقية المقترحة.
